class HandleInfo {
  int idx;
  int type;
  String title;
  String subscribe;
  String material;
  String colorName;
  String thumbPath;

  HandleInfo(this.idx, this.type, this.title, this.subscribe, this.material,
      this.colorName, this.thumbPath);
}
